using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DynamicTile : MonoBehaviour
{
    public float maxSpeedLimit;
    // use global state machine to keep track of speed limit types
    // use the speed limit to determine urban activity by time

    // cluster urban activity by groups of tiles
    // set up radius 

    // how to cluster street tile groups to generate cohesive activity that updates tile groups 
    // how to allow computer to recognize tiles?

    public void setTileConfiguration()
    {

    }

    public void UpdateTileConfiguration()
    {
         
    }

    public void modifySpeedLimit()
    {

    }

    // let the tiles dictate the speed limit 
    // which dictate the AI agents 
}
